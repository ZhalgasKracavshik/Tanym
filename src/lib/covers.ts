/**
 * Обложки по умолчанию для видов событий.
 *
 * Раньше карточка без картинки показывала цветную плашку с иконкой. Это
 * честно закрывало пустоту, но все карточки одного вида выглядели
 * одинаково, и афиша читалась как таблица, а не как афиша. Готовая
 * иллюстрация решает это до того, как администратор что-то загрузил, —
 * а он загружает редко и не сразу.
 *
 * Иллюстрации абстрактные и без надписей намеренно: на карточке уже есть
 * заголовок и плашка статуса, а текст внутри картинки спорил бы с ними и
 * не переводился бы на казахский и английский. Правый верхний угол у всех
 * оставлен пустым — там лежит плашка статуса регистрации.
 */

import type { EventType } from './events';

export const DEFAULT_EVENT_COVER: Partial<Record<EventType, string>> = {
  olympiad: '/covers/olympiad.jpg',
  contest: '/covers/contest.jpg',
  course: '/covers/course.jpg',
  // school — обложки пока нет, вид события остаётся на цветной плашке.
};
