import { redirect } from 'next/navigation';

/**
 * Учебный план переехал в кабинет.
 *
 * Кабинет отвечает на вопрос «где я», план — на «что дальше», и держать
 * половинки одного вопроса в разных пунктах меню значило гонять ученика
 * между ними. Адрес оставлен редиректом: на него ведут закладки, ссылки
 * в письмах напоминаний и переходы из старых версий интерфейса.
 */
export default function PlanPage() {
  redirect('/dashboard');
}
